# 钢4 验证清单（8 项）

> 每项 = 证据命令 + 期望输出。全部通过才允许宣称完成。发布场景追加第 8 项路由 hoi4-publishing。

## 1. error.log 零新增

- [ ] 清空 error.log → `-debug` 启动进主菜单 → 退出 → 重读 error.log
- 期望：无本 mod 相关条目

## 2. 本地化覆盖 100%

- [ ] 所有新 key 在 yml 有条目（无 undefined / 无原始 key 显示）
- [ ] 查 `logs/text.log` 无 key 冲突（loc key collisions）

## 3. GFX 注册核对

- [ ] 每个 icon = `GFX_xxx` 在 `interface/*.gfx` 有同名 spriteType
- [ ] 大小写敏感核对

## 4. BOM 编码检查

- [ ] `.yml` 带 BOM + 首行 `l_simp_chinese:`
- [ ] `.txt` / `descriptor.mod` 无 BOM
- [ ] `.mod`（根目录）带 BOM

## 5. 版本语法核对

- [ ] `supported_version` 两位点（`"1.17.*"` / `"1.17.3"`）
- [ ] 无 1.12/1.14/1.16 旧语法混用

## 6. 重复 id + 占位符扫描

- [ ] 无重复国家 tag
- [ ] 无未替换 TAG / TBD / TODO

## 7. playtest 清单

- [ ] 开局加载正常（无 CTD）
- [ ] 目标国策可点击触发
- [ ] 事件正确弹出
- [ ] 决议可用
- [ ] 存档/读档正常
- [ ] 大型 mod：多轮平衡观察

## 8. 发布场景（仅发布时）

- [ ] 路由 **hoi4-publishing**：descriptor 一致性（.mod 与 descriptor.mod 的 name/version/supported_version）
- [ ] thumbnail、tags、remote_file_id 就绪
